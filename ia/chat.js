const box=document.querySelector("#messages");let sig="";function esc(s){return s.replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}async function load(){const r=await fetch("/api/me");if(r.status===401)return location.href="/";const d=await r.json();name.textContent=d.user.first_name+" "+d.user.last_name;const s=d.messages.map(x=>x.id+x.content).join();if(s===sig)return;sig=s;box.innerHTML=d.messages.map(x=>`<div class="msg ${x.sender}">${esc(x.content)}</div>`).join("");box.scrollTop=box.scrollHeight}document.querySelector("#send").addEventListener("submit",async e=>{e.preventDefault();const v=content.value.trim();if(!v)return;content.value="";await fetch("/api/messages",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({content:v})});load()});load();setInterval(load,2500)